#include <stdio.h>
int main(){
    int a,i,isprime=1;
    scanf("%d",&num);
    for(i=2;i<num;i++){
        if(num%i==0);
        break;
    }
    if(isprime){
        printf("Prime");
    }
    else{
        printf("Not Prime");
        return 0;
    }
}